# Changelog

## Unreleased

- api(events): Return structured validation errors from `POST /api/events` — responses now include an `error` string and optional `missing` array and `details` object for easier client handling.
